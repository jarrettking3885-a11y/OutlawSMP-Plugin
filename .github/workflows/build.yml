name: Debug OutlawSMP

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show files
        run: |
          echo "WHERE AM I?"
          pwd
          echo "ROOT FILES:"
          ls -la
          echo "ALL POM FILES:"
          find . -name "pom.xml"
